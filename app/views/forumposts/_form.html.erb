<%= form_for(@forumpost) do |f| %>
  <% if @forumpost.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@forumpost.errors.count, "error") %> prohibited this forumpost from being saved:</h2>

      <ul>
      <% @forumpost.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :content %><br />
    <%= f.text_area :content %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br />
    <%= f.number_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :topic_id %><br />
    <%= f.number_field :topic_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
